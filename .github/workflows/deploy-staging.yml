name: Deploy [main] to Staging

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  call:
    name: Build and Push
    uses: ledgelabs/ledge-chat/.github/workflows/deploy.yml@main
    with:
      env: staging
